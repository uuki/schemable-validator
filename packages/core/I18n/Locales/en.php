<?php

// English locale preset. Intentionally empty: English default text is supplied
// by the engine-neutral canonical catalog (I18n/DefaultMessages.php), which the
// executable passes as the resolve() fallback. MessageDict::en() therefore yields
// the canonical English messages without duplicating them here. Add entries only
// to override a canonical default for the English locale.
return [];
