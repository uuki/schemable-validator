{
  "name": "integer field accepts numeric string '42'",
  "category": "parity",
  "knownMismatch": true,
  "note": "BE (respect v::intType()) requires a native PHP integer and rejects the numeric string \"42\". FE checkType('integer') already accepts numeric strings via Number() coercion, so FE is_valid=true matches expected but BE returns false. Target = Coercion Contract v1 IntegerCoercion makes BE accept numeric strings too (resolved in W2).",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "age": { "type": "integer" }
    },
    "required": ["age"]
  },
  "input": { "age": "42" },
  "expected": {
    "age": { "is_valid": true }
  }
}
