{
  "name": "hostname format accepts a well-formed domain",
  "category": "contract",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "host": { "type": "string", "format": "hostname" }
    },
    "required": ["host"]
  },
  "input": { "host": "example.com" },
  "expected": {
    "host": { "is_valid": true }
  }
}
